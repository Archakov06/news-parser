{
  "name": "news-parser",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d dist",
    "run": "node dist/index.js",
    "build-run": "babel src --out-dir dist && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build-run && git add . & git commit -m 'update posts' && git push origin master"
  },
  "author": "Archakov Dennis",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "babel-register": "^6.24.1"
  },
  "dependencies": {
    "axios": "^0.16.2",
    "json-beautify": "^1.0.1",
    "jsonfile": "^3.0.0",
    "unirest": "^0.5.1"
  }
}
